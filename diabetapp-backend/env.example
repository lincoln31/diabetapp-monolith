# Configuración del servidor
NODE_ENV=development
PORT=3000

# JWT Secret (genera uno seguro con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=tu_secreto_jwt_muy_largo_y_seguro_de_al_menos_32_caracteres

# Base de datos
DATABASE_URL="postgresql://usuario:contraseña@localhost:5432/diabetapp?schema=public"

# Otras configuraciones
CORS_ORIGIN=http://localhost:3000
