{
  "compilerOptions": {
    /* --- Configuración del Proyecto --- */
    "target": "ES2020",                // Compila a una versión moderna de JavaScript compatible con Node.js reciente.
    "module": "commonjs",              // El sistema de módulos estándar de Node.js ('require').
    "outDir": "./dist",                // Dónde guardar los archivos JavaScript compilados (para producción).
    "rootDir": "./src",                // Dónde se encuentra tu código fuente TypeScript.

    /* --- Interoperabilidad de Módulos --- */
    "esModuleInterop": true,           // LA MÁS IMPORTANTE: Permite importar módulos CommonJS (como Express) con la sintaxis 'import from'. Resuelve muchos problemas.
    "forceConsistentCasingInFileNames": true, // Evita errores de mayúsculas/minúsculas en los nombres de archivo.

    /* --- Chequeo de Tipos Estricto --- */
    "strict": true,                    // Habilita todas las comprobaciones de tipo estrictas. Es una buena práctica.
    "skipLibCheck": true,               // No comprueba los tipos de las librerías de terceros (acelera la compilación).
    "typeRoots":["./src/types", "./node_modules/@types"],
    "types": ["node", "express"]
  },
  "include": [
    "src/**/*"
                             // Le dice a TypeScript que solo compile los archivos dentro de la carpeta 'src'.
  ],
  "exclude": [
    "node_modules"                     // Ignora explícitamente la carpeta node_modules.
  ]
}